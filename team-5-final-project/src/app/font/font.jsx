import { Domine, Arimo } from "next/font/google";

export const domine = Domine({
  variants: ["400", "700"],
  subsets: ["latin-ext"],
  display: "swap",
});

export const arimo = Arimo({
  variants: ["400", "700"],
  subsets: ["latin-ext"],
  display: "swap",
});
